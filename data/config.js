/* ============================================================
   config.js — 게임 기본 설정

   ★ People팀이 가장 자주 바꾸게 될 값들입니다.
     숫자만 바꾸면 되고, 다른 파일은 건드릴 필요 없습니다.
     (JSON 형식과 같습니다. 쉼표 / 따옴표만 지워지지 않게 주의)
   ============================================================ */

window.DRB_CONFIG = {

  /* ---------- 화면에 뜨는 이름 ---------- */
  gameTitle:   "80년의 여정 : 그때, 당신이라면?",
  eduTheme:    "DRB의 여정 : 여기, 그리고 다음",
  subtitle:    "2026 신규입사자 온보딩 · 경영 시뮬레이션",

  /* ---------- 조 편성 ---------- */
  teamCountDefault: 4,               // 기본 조 수
  teamCountOptions: [3, 4, 5, 6],    // 시작화면에서 고를 수 있는 조 수
  teamNames: ["1조", "2조", "3조", "4조", "5조", "6조"],

  /* ---------- 실물 토큰 ---------- */
  tokenUnit: 10,        // 토큰 1개 = 10  (투자는 이 단위로만 올리고 내립니다)

  /* ---------- 예산 ----------
     국면마다 쓸 수 있는 돈은 '고정 예산 + 지난 국면 성과' 입니다.
     고정 예산은 rounds.js 의 국면마다 적혀 있습니다 (50 · 60 · 80 · 60 · 80 · 80).
     회사를 잘 굴려 이익을 냈으면 다음 국면에 더 쓸 수 있습니다.

       성과분 = 고정예산 × (지난 국면 영업이익률 − baseMargin) × perPoint

     ★ 늘어나는 쪽은 크게, 줄어드는 쪽은 작게 잡았습니다.
       못한 조에게서 예산까지 뺏으면 따라잡을 방법이 사라지고,
       그때부터 그 조는 남은 국면을 구경만 하게 됩니다.
       (maxDown 을 0 으로 두면 예산이 줄어드는 일은 아예 없어집니다)

     예) 고정 60 · 지난 이익률 30%  →  60 × (0.30−0.10) × 1.5 = +18  →  78 → 80
         고정 60 · 지난 이익률 −15% →  −10% 에서 멈춤              →  54 → 50 */
  budget: {
    baseMargin: 0.10,   // 이 영업이익률이면 고정 예산 그대로
    perPoint:   1.5,    // 이익률이 기준에서 1%p 멀어질 때마다 예산 1.5%
    maxUp:      0.40,   // 아무리 잘해도 고정 예산의 +40% 까지
    maxDown:    0.10    // 아무리 못해도 −10% 까지만 (여기를 0 으로 두면 안 깎입니다)
  },

  /* ---------- 회사 시작 상태 ----------
     현금만 금액이고, 나머지는 0~100 지수입니다. */
  initialState: {
    cash:     100,
    capacity:  40,   // 생산능력
    tech:      30,   // 기술력
    quality:   40,   // 품질
    trust:     40,   // 고객신뢰
    people:    50    // 인력·조직
  },

  /* ---------- 항상 보여줄 핵심 지표 (사이드 패널) ---------- */
  metrics: [
    { key: "cash",     name: "현금",     max: 260, unit: "억",  desc: "투자와 생존의 밑천" },
    { key: "capacity", name: "생산능력", max: 100, unit: "",    desc: "만들 수 있는 최대량" },
    { key: "tech",     name: "기술력",   max: 100, unit: "",    desc: "제품을 만들 수 있는 수준" },
    { key: "quality",  name: "품질",     max: 100, unit: "",    desc: "불량과 클레임을 좌우" },
    { key: "trust",    name: "고객신뢰", max: 100, unit: "",    desc: "주문이 들어오는 이유" },
    { key: "people",   name: "인력",     max: 100, unit: "",    desc: "모든 투자의 실행력" }
  ],

  /* ---------- 상세보기에서만 보이는 지표 ---------- */
  detailMetrics: [
    { key: "rdStock", name: "R&D 축적",     desc: "일정 수준 이상 쌓이면 기술력이 도약합니다" },
    { key: "fatigue", name: "조직피로도",   desc: "높을수록 품질과 생산성이 떨어집니다" },
    { key: "reach",   name: "영업·거래처",  desc: "수요를 실제 주문으로 바꾸는 힘" }
  ],

  /* ---------- 소리 ----------
     ⚠ 회사 보안정책이 공유폴더에 .mp3 / .wav / .ogg 쓰기를 막습니다.
       그래서 파일을 .m4a 로 넣어두고, 게임이 내용을 보고 알아서 재생합니다.
       (확장자와 실제 형식이 달라도 됩니다)

     배경음악은 딱 세 곡입니다. 시대가 아니라 '무엇을 하는 중인가' 로 갈립니다.
       music-normal.m4a  평상시   국면 시작부터 선택이 끝날 때까지 (6개 국면 모두 같은 곡)
       music-lapse.m4a   시대흐름 선택이 끝나고 연도가 넘어가는 구간
       music-ending.m4a  엔딩     2026 이후 엔딩 · 최종 · 회고
       shock.m4a         Alarm Clock (돌발상황 효과음, 한 번만)

     곡을 바꾸려면 파일만 같은 이름으로 덮어쓰면 됩니다.
     ★ 세 곡 다 무한 반복입니다. 2분짜리 곡이라도 5분 국면 내내 끊기지 않습니다. */
  audio: {
    music: {
      normal: "assets/audio/music-normal.m4a",
      lapse:  "assets/audio/music-lapse.m4a",
      ending: "assets/audio/music-ending.m4a"
    },

    /* 어느 화면에서 어느 곡을 트는가. 여기 적히지 않은 화면은 normal 입니다.
       화면 하나를 다른 곡으로 옮기고 싶으면 이 표에서만 바꾸세요. */
    musicByScreen: {
      roundOpen: "normal",  situation: "normal",  invest: "normal",  policy: "normal",
      timelapse: "lapse",   event:     "lapse",   result: "lapse",   actual: "lapse",
      ending:    "ending",  final:     "ending",  reflect: "ending"
    },

    shock: "assets/audio/shock.m4a",   // 돌발상황이 뜨는 순간 한 번

    /* 첫 화면에 깔리는 영상. 없으면 그냥 딥퍼플 배경입니다.
       3초짜리 짧은 영상이라 <video loop> 로 계속 반복됩니다.
       ⚠ 보안정책상 .mp4 는 이 폴더에 못 넣습니다. .m4v 또는 .webm 으로 넣으세요.
         (내용은 mp4 그대로여도 됩니다 — 게임이 알아서 읽습니다) */
    introVideo: "assets/video/intro.m4v",

    volume:      0.22,   // 평상시 배경음악 (토론을 방해하지 않을 만큼)
    duckVolume:  0.07,   // 돌발상황 동안 배경음악을 이만큼으로 낮춥니다
    shockVolume: 0.55    // 알람
  },

  /* ---------- 맺음말 ----------
     시상까지 끝난 뒤 빔에 띄우는 마지막 화면입니다.
     한 글자씩 찍히고, 빈 줄("")은 한 박자 쉽니다.
     문구를 바꾸려면 이 배열만 고치세요. */
  closing: {
    charMs: 55,     // 글자 하나 찍히는 간격
    lineMs: 700,    // 줄과 줄 사이
    lines: [
      "여러분이 지나온 80년은",
      "여섯 번의 결정이었습니다.",
      "",
      "지금은 하루에도 여섯 번씩",
      "결정해야 합니다.",
      "",
      "앞은 그때보다 더 안 보이고",
      "정답은 더 빨리 바뀝니다.",
      "",
      "그래서 더 빨리 움직이고,",
      "더 많이 서로를 돕습니다.",
      "",
      "바뀌면 다시 맞추고,",
      "맞추면서 한 뼘 더 큽니다.",
      "",
      "DRB는 지금 이 순간에도",
      "여러 갈래로 움직이고 있습니다.",
      "",
      "그 다음 한 걸음은",
      "여러분의 몫입니다."
    ]
  },

  /* ---------- 진행 시간 ----------
     진행자 화면의 '진행 방법' 카드에 그대로 나옵니다.
     시간을 바꾸려면 여기 숫자만 고치세요.
     name 의 `|` 는 화면에서 줄바꿈입니다. */
  totalMinutes: 60,
  phasePlan: [
    { name: "상황 보고|대화",      minutes: 5 },
    { name: "토큰으로|자원 배분",   minutes: 2 },
    { name: "경영정책|하나 선택",   minutes: 1 },
    { name: "결과 공개|조별 비교",  minutes: 1 },
    { name: "DRB는|이렇게 했다",    minutes: 1, fact: true }
  ],

  /* ---------- 시간 관리 ---------- */
  timer: {
    enabled: true,
    discussSeconds: 180,   // 투자 배분 토론 권장 시간 (3분)
    warnSeconds: 45        // 남은 시간이 이보다 적으면 빨간색
  },

  /* ---------- 시뮬레이션 엔진 계수 ----------
     ★ 게임이 너무 쉽거나 너무 어려우면 여기를 조절합니다.
        (자세한 의미는 README의 '난이도 조절' 참고) */
  engine: {
    revenueScale:   4.0,    // 매출 크기. 키우면 돈이 잘 돌고, 줄이면 빡빡해집니다
    baseUnitCost:   0.60,   // 기본 원가율
    fixedCostPerCapacity: 0.14,
    fixedCostPerPeople:   0.16,
    rdStockPerTech: 34,     // R&D가 이만큼 쌓일 때마다 기술력이 오릅니다
    techJumpAmount:  6,
    utilizationWarn: 0.97,  // 가동률이 이보다 높으면 품질이 깎입니다
    fillPenaltyBelow: 0.80, // 납기 충족률이 이보다 낮으면 고객신뢰가 깎입니다
    qualityDecay:    1.0,   // 매 턴 저절로 떨어지는 품질
    trustFloor:     10,     // 고객신뢰가 0까지 무너지면 회복이 불가능해지므로 최소선을 둡니다
    policyChangePenalty: 0.85, // 정책을 바꾼 턴의 투자 효율 (15% 손실)
    policyChangeFatigue: 8,

    /* 실패는 탈락이 아니라 조건 변화 — 현금이 이 아래로 가면 자산을 팔아 버팁니다.
       (어떤 조도 마지막 2026년까지 의미 있는 결정을 계속할 수 있어야 합니다) */
    rescueBelow: -30,

    /* 선두 독주 방지 — 생산능력이 이 값을 넘으면 관리비가 비선형으로 붙습니다.
       "큰 기업은 강하지만 무겁다" */
    sizeCostFrom: 70,
    sizeCostWeight: 260,

    /* 조들이 같은 분야에 몰렸을 때 수요가 깎이는 정도 (0 ~ 0.35).
       모든 조가 같은 곳에 걸면 이 비율만큼 수요를 잃습니다.
         0     경쟁 없음 (조끼리 서로 영향을 주지 않음)
         0.18  기본 — 몰리면 확실히 아프지만 회복 가능
         0.30  치열함. 조가 5~6개일 때 권장
       ⚠ 순위는 진행자 화면에만 나옵니다. 참가자 화면에는 나오지 않습니다. */
    crowdingPenalty: 0.18
  },

  /* ---------- 조 안에서 나눠 맡는 관점 (진행자 가이드) ----------
     강제 규칙이 아니라 토론이 한 사람에게 쏠리지 않게 하는 장치입니다. */
  roles: [
    { name: "시장 관점", question: "이 시장은 앞으로 커질까요, 줄어들까요?" },
    { name: "기술 관점", question: "우리 기술로 이걸 할 수 있습니까?" },
    { name: "재무 관점", question: "이걸 하고도 회사가 버팁니까?" },
    { name: "고객 관점", question: "고객이 정말 원하는 것이 이것입니까?" }
  ],

  /* ---------- 저장 ---------- */
  storage: {
    // 브라우저 localStorage에 저장합니다. 여러 PC 사이에서 합쳐지지 않습니다.
    // 조별 결과를 모으려면 진행자 화면(facilitator.html)에 '결과 코드'를 붙여넣습니다.
    key: "drb_sim_v1"
  },

  /* ---------- 안내 문구 ---------- */
  notices: {
    factVsSim: "금색 카드의 텍스트·연표는 확인된 DRB 기록입니다. 이미지는 AI로 제작한 시대 연출이며 실제 사진이 아닙니다.",
    tokenGuide: "화면에서 숫자를 올리기 전에, 테이블 위 실물 토큰을 먼저 옮기세요."
  }
};
